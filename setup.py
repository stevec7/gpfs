try:
    from setuputils import setup
except:
    from distutils.core import setup


    
setup(
    name = 'gpfs',
    version = '0.0.1',
    description = 'libraries for GPFS',
    author = 'stevec7',
    author_email = 'none',
    packages = ['gpfs']






)
